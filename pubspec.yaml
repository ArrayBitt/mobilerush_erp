name: stock_count
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.5.0
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mobile_scanner: ^7.0.1
  permission_handler: ^11.3.0
  dropdown_search: ^5.0.6
  flutter_launcher_icons: ^0.10.0  # <-- เพิ่มสำหรับสร้าง app icon

flutter:
  uses-material-design: true

  assets:
    - assets/icon/logo.png
    - assets/icon/somjai.png

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/stock_logo.png"  # <-- ไอคอนหลักของแอป
